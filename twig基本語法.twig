{# twig基本語法 #}

{# extends 在某個twig中延展使用領一個twig網頁，可以使用路徑導向 #}
{% extends 'XX.twig' %} 

{# 若想要以html語言輸入，可以在開頭輸入這串代碼，將會把twig改為html的方式呈現 #}
<!DOCTYPE html>

{# head為標頭，不會顯示在網頁內的東西 #}
  <head>

{# <title> 瀏覽器分頁的標題，不是網頁內的 #}
    <title>大樓管理測試頁面</title>

{# 如果想導入css，可以在這裡添加 #}
    <link rel='stylesheet' href='/stylesheets/style.css' />

{# 這個是bootstrap的鏈接方式，documentation裡面有教程，參照教材內的操作即可 #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>
  </head>


{# block相等於php/html中body的存在，是用於放置網頁內容的，結束時一定要用endblock，否則不完整 #}
{% block body %}

{# <h1>代表heading 1的意思，是用於調整字體版型大小的功能 
   <h2>也是一樣的意思，就是heading 2，數字越大，字形越大
   h1,2,3,4,5 通常是用於加重當前字形的重要性
   字體越大，想被看到的重要性越高 #}
  <h1>{{message}}</h1>
  <h2>{{error.status}}</h2>

{#<pre> 這是可以完整保留所有文字信息，不會被任何版型改變 #}
  <pre>{{error.stack}}</pre>

{# href 是將使用者導向到特定網頁，如下則將帶到/index #}
  <a href="/index"></a>

{% endblock %}
