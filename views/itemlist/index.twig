{% extends 'layout.twig' %}
{% block body %}
    <div class="col-sm-9">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>#</th>
                    <th>房號</th>
                    <th>名字</th>
                    <th>聯絡</th>
                    <th> </th>
                </tr>
            </thead>
            <tbody>
                {% for row in rows %}
                <tr>
                    <td>{{row['id']}}</td>
                    <td>{{row['itemcode']}}</td>
                    <td>{{row['price']}}</td>
                    <td>{{row['quantity']}}</td>
                    <td><button type="button" id="edit-itemlist-{{row['id']}}" class="btn btn-outline-secondary btn-sm">Edit</button><td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    <div id="itemlist"><div>
{% endblock %}
